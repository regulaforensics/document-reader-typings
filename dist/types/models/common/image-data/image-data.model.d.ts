/**
* Image data in base64 format
*/
export interface iImageData {
    /**
    * Image data in base64 format
    * @type {string}
    */
    image: string;
}
/**
* Image data in base64 format
*/
export declare class ImageData implements iImageData {
    /**
    * Image data in base64 format
    * @type {string}
    */
    image: string;
}
