import { eLights } from '../lights.const';
/**
* Get lights array from value
* @param {number} value - value
* @returns {eLights[]}
*/
export declare const getLightsArray: (value: number) => eLights[];
